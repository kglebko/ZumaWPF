<UserControl x:Class="ZumaWPF.Views.MainMenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Background="#2C3E50">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" 
                   Text="ZUMA" 
                   FontSize="72" 
                   FontWeight="Bold" 
                   Foreground="#3498DB"
                   HorizontalAlignment="Center" 
                   VerticalAlignment="Center"
                   Margin="0,50,0,0"/>
        
        <StackPanel Grid.Row="1" 
                    HorizontalAlignment="Center" 
                    VerticalAlignment="Center"
                    Margin="0,20">
            <Button Content="Новая игра" 
                    Click="NewGame_Click" 
                    Width="250"
                    Margin="5"/>
            <Button Content="Продолжить" 
                    Click="Continue_Click" 
                    Width="250"
                    Margin="5"
                    Name="ContinueButton"/>
            <Button Content="Выбор уровня" 
                    Click="SelectLevel_Click" 
                    Width="250"
                    Margin="5"/>
            <Button Content="Рекорды" 
                    Click="HighScores_Click" 
                    Width="250"
                    Margin="5"/>
            <Button Content="Справка" 
                    Click="Manual_Click" 
                    Width="250"
                    Margin="5"/>
            <Button Content="Выход" 
                    Click="Exit_Click" 
                    Width="250"
                    Margin="5"/>
        </StackPanel>
        
        <TextBlock Grid.Row="2" 
                   Text="Управление: мышь - наведение и стрельба, ESC - пауза" 
                   FontSize="12" 
                   Foreground="LightGray"
                   HorizontalAlignment="Center" 
                   VerticalAlignment="Bottom"
                   Margin="0,0,0,20"/>
    </Grid>
</UserControl>

