<UserControl x:Class="ZumaWPF.Views.HighScoresView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Background="#2C3E50">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" 
                   Text="ТАБЛИЦА РЕКОРДОВ" 
                   FontSize="36" 
                   FontWeight="Bold" 
                   Foreground="White"
                   HorizontalAlignment="Center" 
                   Margin="0,30"/>
        
        <ListView Grid.Row="1" 
                  Name="ScoresList"
                  Background="#34495E"
                  Foreground="White"
                  Margin="50,20"
                  BorderThickness="0">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Место" Width="80">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Index}" 
                                           HorizontalAlignment="Center"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="Имя" Width="200" DisplayMemberBinding="{Binding Username}"/>
                    <GridViewColumn Header="Очки" Width="150" DisplayMemberBinding="{Binding Score}"/>
                    <GridViewColumn Header="Уровень" Width="100" DisplayMemberBinding="{Binding Level}"/>
                    <GridViewColumn Header="Дата" Width="200" DisplayMemberBinding="{Binding Date, StringFormat='{}{0:dd.MM.yyyy HH:mm}'}"/>
                </GridView>
            </ListView.View>
        </ListView>
        
        <Button Grid.Row="2" 
                Content="Назад" 
                Click="Back_Click" 
                Width="150"
                HorizontalAlignment="Center"
                Margin="0,20"/>
    </Grid>
</UserControl>

