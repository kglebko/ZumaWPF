<UserControl x:Class="ZumaWPF.Views.GameScreenView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1000">

    <Grid>
        <!-- Canvas с фоном -->
        <Canvas Name="GameCanvas" 
                MouseMove="GameCanvas_MouseMove"
                MouseLeftButtonDown="GameCanvas_MouseLeftButtonDown"
                ClipToBounds="True"
                Width="1000"
                Height="800"
                Focusable="True"
                KeyDown="GameCanvas_KeyDown">

            <!-- Фоновое изображение -->
            <Canvas.Background>
                <ImageBrush ImageSource="/Assets/Images/game_bg.jpg" 
                            Stretch="UniformToFill"/>
            </Canvas.Background>

        </Canvas>

        <!-- Панель с очками и уровнем -->
        <Border Background="#80000000"
                Padding="10"
                CornerRadius="6"
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                Margin="10">

            <StackPanel>
                <TextBlock Name="ScoreText"
                           Text="Очки: 0"
                           FontSize="18"
                           Foreground="White"
                           Margin="0,0,0,5"/>

                <TextBlock Name="LevelText"
                           Text="Уровень: 1"
                           FontSize="16"
                           Foreground="White"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>